[pytest]
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (require the ISOLATED test
        stack — `docker compose -f docker-compose.test.yml up -d --build` —
        never the demo stack). See README.md "Testing" section.
testpaths = tests
